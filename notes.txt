PROMPT:

1 =====================
audit the project and look for:
* dead code
* unused exports
* performance improvements
* suggested features
* code quality improvements
* jsdoc coverage
* index.d.ts sync
* tests coverage



PIDS=$(ss -ltnp | grep -E ':5173|:5174' | sed -n 's/.*pid=\([0-9]*\).*/\1/p' | sort -u) || true; if [ -n "$PIDS" ]; then echo "Killing PIDs: $PIDS"; echo $PIDS | xargs -r kill -9; else echo "No dev server PIDs found"; fi

npm run dev:example  



get fresh copy from github-MAIN:

git fetch origin && git checkout main && git reset --hard origin/main



>> Replace silent catches with structured logging or rethrow where appropriate. Replace broad empty catch {} 
>> blocks with catch (e) { /* log or comment */ } so future debuggers have context.

>> client:733 [vite] connecting...
>> client:827 [vite] connected.
>> nimbi-cms.js:5586 [seoManager] compute canonical failed ReferenceError: normalizePath is not defined
>>     at ra (nimbi-cms.js:5577:19)
>>     at sa (nimbi-cms.js:5648:7)
>>     at Yt (nimbi-cms.js:6123:5)
>>     at async tt (nimbi-cms.js:6138:7)
>>     at async ha (nimbi-cms.js:6143:44)
>> ra @ nimbi-cms.js:5586
>> sa @ nimbi-cms.js:5648
>> Yt @ nimbi-cms.js:6123
>> await in Yt
>> tt @ nimbi-cms.js:6138
>> ha @ nimbi-cms.js:6143
>> await in ha
>> (anonymous) @ index.html:59

>> Add caching/throttling for codeblocksManager.loadSupportedLanguages and avoid repeated CDN attempts for same 
>> candidate. codeblocksManager.registerLanguage() attempts multiple dynamic imports (local, CDN) with nested 
>> try/catch — necessary for resilience but expensive. Cache negative results and avoid repeated CDN attempts 
>> for the same candidate.

>> Add short-term TTL/backoff for negative cache entries (e.g., avoid retries for N minutes) 

>> Centralize worker handling (create src/worker-manager.js) to remove duplicated lifecycle code in markdown.js 
>> and slugManager.js and  address broader runtime failures seen in tests. Move additional duplicated worker-related logic (if any) into the manager or add a shared helper for blob→Worker creation.
>> Add a small unit test for worker-manager to assert timeout, error handling, and normal messaging behavior.

>> parseMarkdownToHtml() creates a DOMParser() per call — consider reusing a shared parser instance in long-running 
>> contexts. (Compare with shared SHARED_DOM_PARSER usage in nimbi-cms.js.)


>> Refactor nimbi-cms.js into smaller modules: nav.js, init.js, ui.js. This makes testing and coverage easier. 
>> Work per module and perform all these tasks for each module. Do not start with the next module till you finish the 
>> current one. I need to check in the browser there is no regression:
>> * do not add syntax errors
>> * make the proper wiring, minimize the changes in existing modules
>> * remove unused imports
>> * add proper jsdoc comments
>> * add unit tests, run full test suite
>> * check all the options of initcms are validated

>> home link in nav bar forces a refresh instead of just loading the home page like any other link in the nav bar

>> clean unused imports

>> review index related options effectiveSearchEnabled searchIndexMode

>> why rewriteAnchors function is called in htmlbuilder when we have anchor worker?

>> Audit all exported functions and add/upgrade JSDoc comments, especially for parameters and return types. 
>> Then rerun npm run gen-dts to regenerate accurate types. Take care of the code block insertion point to avoid
>> breaking the code. Run tests after each module work
>> 
>> Audit all JSDoc blocks to ensure that are properly sit directly above the functions when appropriate, 
>> remove the duplicates, etc. Be craefull not to break the existing code

>> Tighten critical typedefs: Add explicit typedefs for:
>>     slugToMd, mdToSlug — Map<string,string> (slugManager.js:14)
>>     fetchCache, crawlCache — Map types (slugManager.js:338)
>>     SUPPORTED_HLJS_MAP, HLJS_ALIAS_MAP — Map<string,string> / Record types (codeblocksManager.js:13)
>>     parseMarkdownToHtml result type — make ParseResult explicit (markdown.js:86)

>> Some functions have JSDoc, but many public exports in index.d.ts are typed as any. Running npm run gen-dts 
>> then tightening types (or switching to checkJs with JSDoc) will improve DX and catch issues earlier. 
>> See index.d.ts for current gaps. index.d.ts exists and is generated, but many return types are any and some 
>> exported signatures could be more precise (e.g., fetchMarkdown, slug-related types). Consider improving 
>> gen-dts.js and adding type tests.

>> add type tests

>> remove filesManager.js if not needed, update the wiring and run the tests

>> Clean up any lingering docs/typedoc references to filesManager.js
>> Update README.md to remove mention of filesManager.js if it still exists there
>> Run a quick grep to ensure nothing else imports filesManager.js (excluding docs/coverage)

>> Run a static analysis pass for unused exports / dead code, remove circular imports

>> add jsdoc blocks to hookmanager

>> searchIndexMode=off was removed as an option: update the code, jsdoc and readme if needed

* remove excess of inline comments




* Replace silent catches with structured logging or rethrow where appropriate. Replace broad empty catch {} 
  blocks with catch (e) { /* log or comment */ } so future debuggers have context.



* nimbi-cms is higher than nimbi-mount because the nav bar height is not taken into account

* toc should have max height and be scrollable if overflown

* generate proper UMD, taking into account inline workers, dynamic imports, etc.

* remove dynamic imports when theres already a static import for the same module

* import 'highlight.js/styles/monokai.css' in ninbi-cms?

* allow URL parameters





Add lint rule(s):
    no-duplicate-declarations (already built-in ESLint) and enable no-unused-vars/TypeScript noUnusedLocals.
Add eslint-plugin-unused-imports or use TypeScript --noUnusedLocals for catching dead exports.
CI enforcement: Run npm run gen-dts in CI and fail if index.d.ts differs; run tests and a docs/type-check step.
Performance hygiene:
    Defer network calls and heavy work to init functions or workers.
    Add a bundle analysis (e.g., rollup-plugin-visualizer / Vite analyzer) if shipping to browser.
Static analysis: Run madge for circular dependencies and depcheck to find truly unused files not caught in tests.