[**nimbi-cms**](../../README.md)

***

[nimbi-cms](../../README.md) / [slugManager](../README.md) / buildSearchIndexWorker

# Function: buildSearchIndexWorker()

> **buildSearchIndexWorker**(`contentBase`, `indexDepth?`, `noIndexing?`): `Promise`\<`object`[]\>

Build the search index using the slug worker when available.

## Parameters

### contentBase

`string`

contentBase parameter

### indexDepth?

`number` = `1`

### noIndexing?

`undefined` = `undefined`

## Returns

`Promise`\<`object`[]\>

- resolved search index entries
