[**nimbi-cms**](../../README.md)

***

[nimbi-cms](../../README.md) / [slugManager](../README.md) / resolveSlugPath

# Function: resolveSlugPath()

> **resolveSlugPath**(`slug`): `string` \| `null`

Given a slug, return the most appropriate markdown path taking the
current UI language into account.

## Parameters

### slug

`string`

slug parameter

## Returns

`string` \| `null`

- The resolved markdown path for the slug, or `null` if not found.
