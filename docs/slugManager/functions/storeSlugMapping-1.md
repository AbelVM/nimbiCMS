[**nimbi-cms**](../../README.md)

***

[nimbi-cms](../../README.md) / [slugManager](../README.md) / storeSlugMapping

# Function: storeSlugMapping()

> **storeSlugMapping**(`slug`, `rel`): `void`

Public alias for `_storeSlugMapping` used by UI/runtime modules.

## Parameters

### slug

`string`

Slug key to associate with a markdown path.

### rel

`string`

Markdown path (relative to content base) to associate with the slug.

## Returns

`void`
