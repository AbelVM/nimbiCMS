[**nimbi-cms**](../../README.md)

***

[nimbi-cms](../../README.md) / [router](../README.md) / buildPageCandidates

# Function: buildPageCandidates()

> **buildPageCandidates**(`resolved`): `string`[]

Given a resolved identifier (possibly slug, path, or HTML), produce an
ordered list of candidate markdown/html filenames to attempt fetching.

## Parameters

### resolved

`string`

resolved parameter

## Returns

`string`[]

- Array of candidate filenames to attempt fetching.
