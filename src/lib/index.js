/**
 * Library entry re-exports — kept minimal to provide a friendly import
 * surface for consumers that import from `nimbi-cms/lib`.
 *
 * Exported:
 * - `initCMS` (default export from the main module)
 */
export { default as initCMS } from '../nimbi-cms.js'
export { default } from '../nimbi-cms.js'
